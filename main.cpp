#include <algorithm>
#include <functional>

#include <cassert>
#include <cstdio>
#include <cstdlib>
#include <cstring>

#include <string>
#include <vector>

#include "IndexBWA.h"
#include "IndexLocation.h"

using namespace std;

char *bwa_pg;

int main(void)
{
  IndexBWA index;
  index.process("data/ls_orchid.fasta", 1); // It will read in the index if already exists

  char Q[] = "CCATGAA";
  char convertedQuery[123];

  unsigned char nst_nt4_table[256] = {
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 5 /*'-'*/, 4, 4,
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 0, 4, 1,  4, 4, 4, 2,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  3, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 0, 4, 1,  4, 4, 4, 2,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  3, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4, 
    4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4,  4, 4, 4, 4
  };

  for (int i = 0, len = strlen(Q); i < len; ++i)
    convertedQuery[i] = nst_nt4_table[(int)Q[i]];

  unsigned long long numHits;
  IndexLocationList* lista = index.find(convertedQuery, strlen(Q), /* thread id */ 0, &numHits, /* max num hits */ 10);

  printf("number of hits = %llu\n", numHits);
  printf("list size = %lu\n", lista->size());
  IndexLocationList& L = *lista;
  for (unsigned int i = 0; i < L.size(); ++i) {
    printf("seqId = %llu start = %llu rev = %d\n", L[i].sequenceId, L[i].start, (int)L[i].isReverse);
  }

  delete lista;
  return 0;
}
